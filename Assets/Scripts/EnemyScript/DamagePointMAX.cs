using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DamagePointMAX : MonoBehaviour
{
    /*public static Animator animator;
    public static Transform attackEnemyPoint;
    public static float AttackRangeEnemy = 0.5f;
    public static LayerMask Players;
    public GameObject players;

    private void Update()
    {
        if (players.gameObject.layer == LayerMask.NameToLayer("Players"))
        {
            Attack();
        }
    }


    public static void Attack()
    {
        animator.SetTrigger("ATTACK");

        Collider2D[] hitPlayers = Physics2D.OverlapCircleAll(attackEnemyPoint.position, AttackRangeEnemy, Players);

        foreach (Collider2D player in hitPlayers)
        {
            Debug.Log("урон");
        }
    }
    private void OnDrawGizmosSelected()
    {
        if (attackEnemyPoint == null)
            return;

        Gizmos.color = Color.green;
        Gizmos.DrawWireSphere(attackEnemyPoint.position, AttackRangeEnemy);
    }*/
}
